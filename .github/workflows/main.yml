name: Build CPython 3.9 for Nintendo Switch

run-name: ${{ github.actor }} is building CPython for Switch 🚀

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
      with:
        submodules: 'recursive'
    
    - name: Cache CPython sources
      uses: actions/cache@v4
      with:
        path: |
          cpython
          venv
        key: ${{ runner.os }}-cpython-${{ hashFiles('setup.bash', 'cpython.patch') }}
    
    - name: Set up devkitPro
      uses: devkitPro/actions/setup-devkit@v1
      with:
        pacman-packages: |
          switch-dev
          devkitARM
          devkitA64
          libnx
    
    - name: Setup build environment
      run: |
        chmod +x setup.bash
        ./setup.bash
    
    - name: Build CPython for Switch
      run: |
        chmod +x build.bash
        ./build.bash
    
    - name: Build Switch NRO
      run: |
        cd switch
        make
    
    - name: Verify build artifacts
      run: |
        echo "Checking build artifacts..."
        ls -la python39-switch/sd_layout/python/lib/
        if [ -f switch/python_switch.nro ]; then
          echo "✓ NRO file built successfully"
          file switch/python_switch.nro
        else
          echo "✗ NRO file not found!"
          exit 1
        fi
    
    - name: Upload artifacts
      uses: actions/upload-artifact@v4
      with:
        name: python-switch-build
        path: |
          python39-switch/sd_layout/
          switch/python_switch.nro
        retention-days: 7
